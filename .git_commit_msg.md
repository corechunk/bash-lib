build(core): compile monolithic artifacts and update gitignore

- Generated and committed compiled monoliths (`bash-lib` root entry and category artifacts like `lib/core/core`, `lib/ui/ui`, etc.) using the new deployment build scripts.
- Added `tmp/` directory, scratch output files (`*.done`), and `log.txt` to `.gitignore` to prevent tracking of test/temporary execution artifacts.
